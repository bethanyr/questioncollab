<h2>Question</h2>
<h2>Question: <%= @question.title %></h2>
<h2>Answer: <%= @question.answer %></h2>
<h2>Category: <%=@question.category %></h2>

<h2>Users</h2>
<ul id='users'>
  <% @question.users.each do |user| %>
    <li>
      #<%= user.id %> -
      <%= link_to user.email, [@question, user] %>
    </li>
   <% end %>
</ul>

<%= link_to "Edit Question", [:edit, @question] %>
<%= link_to "Delete Question", [@question], :method => :delete, :confirm => "Are you sure you want to delete this question?" %>
<%= link_to "View Questions", root_path %>
