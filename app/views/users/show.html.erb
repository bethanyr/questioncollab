<h3>Edited Questions by <%= current_user.email %></h3>
<ul>
  <% current_user.questions.each do |q| %>
    <li>
      #<%= q.id %> -
      <%= link_to q.title, q %>
    </li>
   <% end %>
</ul>
<%= link_to "Back to Questions", root_path %>
